<UserControl x:Class="VsAgentic.UI.Controls.ChatWebView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <wv2:WebView2 x:Name="WebView"
                      Grid.Row="0"
                      DefaultBackgroundColor="Transparent"/>
        <!-- Banner host: permission banner or question card injected here at
             runtime. Sits below the chat history and above the input box so
             the user can still see what the AI is doing while answering.
             BorderBrush is set from BannerTheme in code-behind. -->
        <Border x:Name="BannerHost"
                Grid.Row="1"
                Visibility="Collapsed"
                Padding="0"
                BorderThickness="0,1,0,0"/>
    </Grid>
</UserControl>
